services:
  - type: web
    name: biblical-wisdom
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    rootDir: ./
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false